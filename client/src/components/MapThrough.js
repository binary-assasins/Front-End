import React from "react"; 

function MapThrough(props) {
    
    return(
        <>
        <div> {props.title} </div> 
        <div> {props.x} </div> 
        <div> {props.y} </div> 
        <div> {props.description} </div> 
        </>
    )
}

export default MapThrough

